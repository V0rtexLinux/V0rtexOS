name: Build AETERNUS OS ISO

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest
      options: --privileged
    env:
      TERM: xterm  # CORREÇÃO DO ERRO DA IMAGEM

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --needed --noconfirm archiso git base-devel libisoburn squashfs-tools dosfstools mtools

      - name: Prepare AETERNUS Build
        working-directory: ./aeternus-os
        run: |
          chmod +x build.sh
          # Limpa resíduos de builds anteriores se existirem
          rm -rf work/
          
      - name: Run Build Script
        working-directory: ./aeternus-os
        run: |
          # Roda o build ignorando avisos de terminal
          ./build.sh

      - name: Generate Checksum
        working-directory: ./aeternus-os
        if: always() # Roda mesmo se o build falhar para vermos o que foi gerado
        run: |
          mkdir -p release
          cd release/
          sha256sum *.iso > checksum.txt || echo "Nenhuma ISO gerada"

      - name: Upload ISO Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: Aeternus-OS-ISO
          path: |
            aeternus-os/release/*.iso
            aeternus-os/release/checksum.txt
          retention-days: 5
